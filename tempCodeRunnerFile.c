while (n != 0)
                {
                    somma = somma + n;
                    scanf("%d", &n);
                }
                printf("La somma di tutti i numeri e': %d\n", somma);